.DS_Store

# Python
backend/.venv/
__pycache__/
backend/__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/dist/

# Caches
cache/
backend/cache/
.cache/
.vite/
frontend/node_modules/.vite/

# Standalone experiment artifacts
outputs/

# Inspection-only export from the v4 similarity precompute. Regenerate with
# scripts/precompute_similarity_v4.py; the backend reads similarity_v4.json.
backend/data/similarity_v4_comps.csv

# Local-dev copy of the first-load payload, with the click-time caches included
# so `npm run dev` needs no backend running. ~108 MB, past GitHub's 100 MB limit.
# The slim default_bootstrap.json beside it IS committed and is what deploys.
# Both are written by scripts/precompute_default_bootstrap.py.
frontend/public/precomputed/default_bootstrap.full.json
frontend/.env*.local
